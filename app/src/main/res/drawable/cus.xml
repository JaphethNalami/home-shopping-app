<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <layer-list>
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="#A1B0BC" /> <!-- Background color when pressed -->
                    <corners android:radius="5dp" /> <!-- Border radius in dp -->
                </shape>
            </item>
            <item android:right="2dp" android:left="2dp" android:top="2dp" android:bottom="2dp">
                <shape android:shape="rectangle">
                    <solid android:color="#010509" /> <!-- Border color (darker) -->
                    <corners android:radius="5dp" /> <!-- Border radius in dp -->
                </shape>
            </item>
        </layer-list>
    </item>
    <item>
        <layer-list>
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="#010509" /> <!-- Default background color -->
                    <corners android:radius="5dp" /> <!-- Border radius in dp -->
                </shape>
            </item>
            <item android:right="2dp" android:left="2dp" android:top="2dp" android:bottom="2dp">
                <shape android:shape="rectangle">
                    <solid android:color="#055290" /> <!-- Border color (lighter) -->
                    <corners android:radius="5dp" /> <!-- Border radius in dp -->
                </shape>
            </item>
        </layer-list>
    </item>
</selector>
